export interface IComponent {
  name: string;
  code: string;
  componentId: string;
  slug: string;
  prompt: string;
  timestamp: string;
  version: string;
  extension: string;
}

export default {} as IComponent;
